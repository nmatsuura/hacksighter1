<h1><p>Edit Sighting</p></h1>

<%#= Topic.find(@sighting.topic_id).name%>


<%= render 'form' %>

	
		<div>
		Created at: <%= @sighting.created_at %><br />
		</div>

<p>
<%= gmaps("markers" => {"data" => @json}) %>
</p>


<%= link_to 'Show', @sighting %>
<%= link_to 'Back', sightings_path %>